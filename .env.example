# Server port (default: 3001)
PORT=3001

# Allowed origin for CORS — set to your frontend dev server URL
CLIENT_ORIGIN=http://localhost:5173

# Absolute path to the SQLite database file (default: server/decisions.db)
# DB_PATH=/absolute/path/to/decisions.db
